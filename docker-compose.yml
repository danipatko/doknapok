version: '1.0'
services:
    web:
        build: app
        ports:
            - 3000:3000
    nginx:
        build: nginx
        ports: 8000:80
    redis:
        image: redislabs/redismod
        ports:
            - 6379:6379
